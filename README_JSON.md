## Export JSON + HTML statico (analisi)

- Home introduttiva: `/`
- Analisi read-only: `/summary/`, `/dashboard_mini/`, `/dashboard_mini_bracket/`, `/stats/`, `/commander_brackets/`
- Triplette: `/triplette/` (JSON + tabella)

### Genera `docs/`
```bash
python export_static_json.py
```

Sostituisci l'immagine `docs/assets/hero.jpg` con la tua.
